github:
  username: your_name
  email: your_email@example.com

scheduler:
  timezone: Asia/Shanghai
  mode: random
  min_commit: 1
  max_commit: 4

generator:
  enabled:
    - heartbeat
    - readme
    - quote

commit:
  style: conventional
